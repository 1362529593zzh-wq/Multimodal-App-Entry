# 后端阶段完成情况

## 当前阶段

截至 2026-04-29，后端已经完成“基础工程 + 配置中心第一版”，并具备前端配置中心联调条件。

当前结论：

- Spring Boot 工程可启动
- PostgreSQL + Flyway 已接通
- 配置中心 4 张核心表已完成第一版 CRUD
- `/api/ping` 与 `/actuator/health` 可访问
- 已提交 Maven Wrapper，可直接使用 `backend/mvnw.cmd`

## 已完成内容

### 1. 基础运行环境

- Spring Boot 3.5.x
- Java 17
- Maven Wrapper
- PostgreSQL 数据源
- Flyway
- MyBatis-Plus
- Actuator

### 2. 数据库迁移

已落地迁移脚本：

- `backend/src/main/resources/db/migration/V1__init_schema.sql`
- `backend/src/main/resources/db/migration/V2__core_tables.sql`
- `backend/src/main/resources/db/migration/V3__extra_tables.sql`
- `backend/src/main/resources/db/migration/V4__indexes.sql`
- `backend/src/main/resources/db/migration/V5__seed_data.sql`

已验证：

- 应用启动时 Flyway 可自动校验并迁移
- 开发库已具备配置中心演示数据

### 3. 最小运行接口

- `GET /api/ping`
- `GET /actuator/health`

### 4. 配置中心第一版接口

#### 功能配置 `mm_function_config`

- `GET /api/config/functions`
- `GET /api/config/functions/{functionCode}`
- `POST /api/config/functions`
- `PUT /api/config/functions/{functionCode}`
- `PATCH /api/config/functions/{functionCode}/enabled`

#### 模型服务 `mm_model_service`

- `GET /api/config/model-services`
- `GET /api/config/model-services/{serviceCode}`
- `POST /api/config/model-services`
- `PUT /api/config/model-services/{serviceCode}`
- `PATCH /api/config/model-services/{serviceCode}/enabled`

#### 功能模型绑定 `mm_function_model_binding`

- `GET /api/config/function-model-bindings`
- `GET /api/config/function-model-bindings/{functionCode}/{serviceCode}`
- `POST /api/config/function-model-bindings`
- `PUT /api/config/function-model-bindings/{functionCode}/{serviceCode}`
- `PATCH /api/config/function-model-bindings/{functionCode}/{serviceCode}/enabled`

#### 参数模板 `mm_param_template`

- `GET /api/config/param-templates`
- `GET /api/config/param-templates/{templateCode}`
- `POST /api/config/param-templates`
- `PUT /api/config/param-templates/{templateCode}`
- `PATCH /api/config/param-templates/{templateCode}/enabled`

## 今天已确认可用的内容

- 4 组配置列表接口都能返回种子数据
- 前端构建通过，接口字段名与页面代码已基本对齐
- 请求失败时可返回统一 `code / message / data / timestamp` 结构

## 当前剩余工作

进入工作台壳子之前，后端只做配置中心联调收口：

1. 配合前端确认新增 / 编辑 / 启停链路
2. 修复字段校验、错误提示、返回细节问题
3. 不扩展到 RabbitMQ、MinIO、Redis 或真实模型接入

## 联调完成后的下一目标

配置中心收口后，后端直接进入工作台最小骨架：

- `mm_chat_session`
- `mm_chat_message`
- `mm_task`

第一阶段目标仅包括：

- 能创建会话
- 能发送一条消息
- 能创建一条任务

也就是先为工作台壳子提供最小闭环，不提前引入真实模型调用链路。
