# 多模态应用入口推荐开发方案

## 1. 文档说明

本文档基于《多模态应用入口需求分析与架构设计》以及当前沟通结论整理，作为项目实施阶段的推荐开发方案。

本次修订在原方案基础上做了两项关键调整：

- 暂不按“平台子应用接入”进行设计，先按独立项目方式完成系统建设
- 数据库选型从 `MySQL` 调整为 `PostgreSQL`

因此，本文档聚焦于“如何将项目完整落地”，重点覆盖：

- 架构方案
- 前后端技术栈
- PostgreSQL 数据设计思路
- 模块划分
- 开发步骤

---

## 2. 最终推荐方案

本项目推荐采用以下总体方案：

**独立多模态应用系统 + 前后端分离 + 后端模块化单体 + 异步任务编排 + 模型适配层隔离 + PostgreSQL 数据治理**

这份方案的核心思想是：

- 采用模块化单体保障一期开发效率和交付稳定性
- 聚焦业务闭环本身，不额外引入平台菜单、微前端、SSO 等接入复杂度
- 保留后续演进边界，方便未来再接平台或继续拆分服务
- 使用 `PostgreSQL` 承载配置、任务、消息、记录、统计等核心数据

可将本方案理解为：

**“以稳妥交付为主，以工程扩展为辅，以 PostgreSQL 为核心数据底座的一期实施方案。”**

---

## 3. 方案选择原因

结合当前需求分析，本项目最适合上述方案，原因如下：

### 3.1 当前阶段重点是先完成业务闭环

当前项目的核心目标不是构建一个复杂的平台体系，而是先完成以下三大中心：

- 模型配置服务中心
- 对话式多模态工作台
- 调用记录与统计中心

与此同时，还要打通五类能力：

- 图像识别
- 图像生成 / 文生图
- 文生语音
- 文生 PPT
- 文生视频

因此，一期更适合选择结构清晰、开发风险低的模块化单体方案。

### 3.2 暂不考虑平台子应用后，方案可以更聚焦

当不再优先考虑平台子应用接入时，可以先去掉以下额外复杂度：

- 微前端挂载
- 主平台菜单接入
- 平台统一鉴权对接
- 主子应用通信
- 平台公共文件中心复用

这样可以把开发重点完全放在：

- 工作台交互
- 模型编排
- 文件下载
- 调用记录
- 统计导出

### 3.3 PostgreSQL 更适合当前数据特点

本项目存在大量半结构化数据和复杂筛选统计需求，典型包括：

- 模型支持参数
- 参数模板配置
- 请求参数快照
- 模型原始响应
- 多模态结果摘要
- 调用记录筛选与聚合分析

相较于 `MySQL`，`PostgreSQL` 在以下方面更适合：

- `jsonb` 支持更强
- 复杂查询能力更灵活
- 聚合统计能力更强
- 适合存储动态配置和异构结果结构

### 3.4 后续演进仍然需要预留边界

虽然当前按独立项目落地，但后续仍可能扩展：

- 更多 AI 能力类型
- 更复杂的多轮上下文能力
- 更大的任务调用量
- 平台接入与统一权限体系

因此一期虽然不做服务拆分，但必须提前预留：

- 任务中心独立演进边界
- 模型适配层独立演进边界
- 记录与统计模块独立演进边界

---

## 4. 推荐技术架构

### 4.1 架构模式

推荐架构模式如下：

- 前后端分离架构
- 独立 Web 应用形态
- 后端模块化单体
- 统一模型适配层
- 同步 + 异步任务混合编排
- 文件与调用记录统一治理
- PostgreSQL 作为核心业务数据库

### 4.2 总体分层

系统建议分为三层：

#### 4.2.1 前端展示层

负责页面交互与用户操作，包含：

- 对话式多模态工作台
- 模型配置服务中心
- 调用记录中心
- 统计分析页面

#### 4.2.2 后端业务编排层

负责业务核心逻辑，包括：

- 功能配置
- 模型服务配置
- 会话与消息管理
- 意图识别与路由
- 任务调度
- 文件管理
- 调用记录
- 统计分析
- 导出能力

#### 4.2.3 能力接入层

负责对接外部能力，包括：

- 已有模型底座
- 模型中心接口
- 对象存储
- 消息队列

---

## 5. 推荐前端技术栈

前端建议采用以下技术栈：

- `React 18`
- `TypeScript`
- `Vite`
- `Ant Design`
- `React Router`
- `Zustand`
- `TanStack Query`
- `Axios`
- `ECharts`

### 5.1 选择理由

选择该技术栈的原因如下：

- React 适合构建复杂对话流和多模态结果组件
- TypeScript 有利于功能、参数、任务、记录等复杂数据结构管理
- Vite 启动快、构建效率高，适合独立中后台应用
- Ant Design 适合列表、抽屉、筛选区、动态表单等企业后台界面
- Zustand 适合会话上下文、功能状态、参数状态管理
- TanStack Query 适合处理异步任务轮询、列表缓存和接口状态管理
- ECharts 适合统计分析页和趋势图展示

---

## 6. 推荐后端技术栈

后端建议采用以下技术栈：

- `Java 17 / 21`
- `Spring Boot 3`
- `Spring Web`
- `Spring Validation`
- `Spring Security`
- `MyBatis-Plus`
- `PostgreSQL 14+`
- `Redis`
- `RabbitMQ`
- `MinIO`
- `EasyExcel`
- `Flyway`

### 6.1 选择理由

- Spring Boot 适合企业内部业务型系统快速落地
- Java 技术栈在配置、任务、记录、导出、审计、运维方面成熟稳定
- PostgreSQL 适合配置类、记录类、统计类和半结构化数据场景
- Redis 用于缓存配置、热点查询、短时任务状态缓存
- RabbitMQ 适合异步任务通知和任务状态解耦
- MinIO 用于统一管理输入附件和输出结果文件
- EasyExcel 用于调用记录和统计报表导出
- Flyway 用于数据库结构版本管理，保证开发、测试、生产环境一致

---

## 7. PostgreSQL 数据库设计建议

### 7.1 PostgreSQL 在本项目中的定位

`PostgreSQL` 作为本项目核心业务数据库，主要承担以下职责：

- 存储功能配置和模型服务配置
- 存储会话和消息数据
- 存储任务状态与执行快照
- 存储调用记录与下载日志
- 支撑统计分析与导出查询

### 7.2 推荐使用方式

建议遵循以下原则：

- 固定字段使用关系型列建模
- 动态配置、请求快照、结果摘要优先使用 `jsonb`
- 高频筛选字段建立普通索引
- `jsonb` 查询字段建立 `GIN` 索引
- 统计类数据优先通过汇总表或日统计表实现

### 7.3 适合使用 `jsonb` 的字段

以下内容建议优先采用 `jsonb` 存储：

- 模型支持参数配置
- 参数模板配置
- 请求参数快照
- 模型原始返回结果
- 多模态输出摘要
- 文件扩展元数据
- 错误详情信息

### 7.4 索引建议

建议重点建立以下索引：

- 按时间查询索引：用于任务、消息、记录列表
- 按状态查询索引：用于任务状态和失败筛选
- 按功能类型查询索引：用于能力分类筛选
- 按模型服务查询索引：用于模型统计与记录筛选
- `jsonb` 字段的 `GIN` 索引：用于动态参数和快照检索

### 7.5 数据库变更管理建议

建议使用 `Flyway` 管理数据库版本，至少包括：

- 初始化建表脚本
- 索引变更脚本
- 字段新增或结构变更脚本
- 初始化基础配置数据脚本

这样可以保证：

- 环境一致
- 结构可追溯
- 升级可回放

---

## 8. 推荐系统模块划分

后端采用模块化单体方式，但内部模块边界要清晰，建议至少拆分为以下模块：

### 8.1 功能配置模块

职责：

- 管理功能条配置
- 配置功能启停、排序、名称、图标
- 配置功能对应默认模型与候选模型
- 配置参数模板

### 8.2 模型服务配置模块

职责：

- 配置模型服务基础信息
- 维护模型服务与功能关系
- 配置前台可见参数项
- 配置服务可用状态、鉴权方式、超时时间

### 8.3 会话与消息模块

职责：

- 管理会话
- 管理消息流
- 支持上下文承接
- 记录对话轮次和消息结构

### 8.4 意图识别与路由模块

职责：

- 在用户未主动选择功能时识别任务意图
- 路由到图像识别、图像生成、文生语音、文生 PPT、文生视频等能力
- 支持意图不明确时的回退机制

### 8.5 任务管理模块

职责：

- 创建任务
- 管理同步任务和异步任务状态
- 处理任务执行编排
- 关联模型调用、文件结果、调用记录

### 8.6 模型适配层

职责：

- 对接已有模型底座
- 屏蔽不同模型协议差异
- 统一请求结构和返回结构
- 支持同步和异步返回模式

### 8.7 文件管理模块

职责：

- 上传参考图和附件
- 保存输出文件
- 生成预览地址和下载地址
- 管理文件元数据和权限信息

### 8.8 调用记录模块

职责：

- 保存每次调用全过程信息
- 提供列表、详情、筛选查询
- 记录所用功能、模型、参数、结果、状态、耗时
- 记录下载动作

### 8.9 统计分析模块

职责：

- 汇总调用次数、成功率、耗时等指标
- 提供能力统计、模型统计、趋势统计
- 支持统计报表导出

### 8.10 导出模块

职责：

- 导出记录列表
- 导出单条调用详情
- 导出统计报表

---

## 9. 关键工程设计建议

### 9.1 任务编排机制

该项目的关键不是普通 CRUD，而是多模态任务编排。

建议采用如下处理机制：

1. 前端提交对话请求或功能请求
2. 后端判断用户是否主动选择功能
3. 若未选择功能，则进入意图识别模块
4. 根据功能配置和模型配置加载默认模型及参数模板
5. 创建任务
6. 调用模型适配层
7. 模型适配层调用已有模型底座
8. 若为同步任务则直接返回结果
9. 若为异步任务则返回任务号并轮询或等待回调
10. 结果保存到文件服务，记录写入调用记录中心
11. 前端根据任务状态将结果回流到对话消息流

#### 9.1.1 建议任务状态

- `CREATED`
- `PENDING`
- `RUNNING`
- `SUCCESS`
- `FAILED`
- `CANCELED`

### 9.2 模型适配层必须独立

模型适配层是保证系统后续可维护性的关键，不建议直接在业务控制器中写模型调用逻辑。

建议统一封装：

- 请求参数转换
- 协议适配
- 鉴权处理
- 返回结构解析
- 错误码转换
- 超时与异常兜底

统一输出建议包含：

- 任务 ID
- 执行状态
- 结果类型
- 文本结果
- 文件结果列表
- 原始响应
- 错误码
- 错误信息
- 执行耗时

### 9.3 文件与下载统一治理

下载能力是验收重点，因此建议文件体系统一设计：

- 输入附件和输出文件统一走对象存储
- 数据库存储文件元数据，不直接存大文件本体
- 预览地址与下载地址分离
- 下载统一由后端鉴权控制
- 下载次数记录到调用记录体系中

### 9.4 PostgreSQL 访问层建议

建议后端数据库访问层按以下规范建设：

- 实体类与数据库表一一对应
- `jsonb` 字段采用统一对象或字符串映射策略
- 复杂筛选查询尽量收敛到独立 Mapper 或 Repository
- 列表查询、详情查询、统计查询分层实现，避免 SQL 混杂
- 调用记录、消息记录、任务记录的查询接口统一支持分页

---

## 10. 推荐数据库对象

建议至少设计以下核心表：

- `mm_function_config`
- `mm_model_service`
- `mm_function_model_binding`
- `mm_param_template`
- `mm_chat_session`
- `mm_chat_message`
- `mm_task`
- `mm_task_result`
- `mm_file_asset`
- `mm_call_record`
- `mm_record_download_log`
- `mm_stats_daily`

### 10.1 设计原则

- 配置与运行数据分离
- 文件与任务分离
- 会话与调用记录分离
- 统计结果做日汇总，避免每次实时重算
- 动态字段优先 `jsonb`
- 高频筛选字段优先显式列建模

### 10.2 建议重点字段类型

建议优先使用以下 PostgreSQL 类型：

- 主键：`bigserial` 或 `uuid`
- 时间：`timestamp` / `timestamptz`
- 动态配置：`jsonb`
- 布尔值：`boolean`
- 状态类：`varchar` 或枚举映射
- 统计类：`integer`、`bigint`、`numeric`

---

## 11. 前端页面结构建议

推荐前端按四个一级页面组织：

### 11.1 对话式多模态工作台

页面组成：

- 左侧导航
- 中间消息流
- 底部输入框
- 底部功能条
- 底部参数快捷项弹层

### 11.2 模型配置服务中心

页面组成：

- 顶部操作区
- 状态统计卡片
- 服务列表
- 搜索筛选区
- 右侧详情抽屉

### 11.3 调用记录中心

页面组成：

- 顶部操作区
- 状态统计卡片
- 搜索筛选区
- 记录列表
- 右侧详情抽屉

### 11.4 统计分析页面

页面组成：

- 统计卡片
- 趋势图表
- 模型排行
- 报表导出按钮

---

## 12. 推荐实施步骤

建议按以下顺序推进项目开发：

### 第一步：需求与数据结构冻结

目标：

- 明确五类能力的统一入参
- 明确统一结果结构
- 明确任务、记录、文件、统计字段
- 明确 PostgreSQL 数据建模边界

输出物：

- 接口字段清单
- 数据对象定义
- 页面功能清单
- 关键表草案

### 第二步：搭建项目基础工程

目标：

- 创建前端工程
- 创建后端 Spring Boot 工程
- 初始化 PostgreSQL、Redis、RabbitMQ、MinIO
- 配置 Flyway 数据库脚本管理

输出物：

- 前后端基础骨架
- 环境配置
- 基础部署结构
- 数据库初始化脚本

### 第三步：完成模型配置服务中心

目标：

- 实现模型服务配置
- 实现功能与模型绑定
- 实现默认模型与候选模型配置
- 实现参数模板配置

输出物：

- 服务配置页面
- 配置管理接口
- 相关基础表

### 第四步：完成对话工作台基础壳子

目标：

- 实现左侧导航
- 实现消息流
- 实现底部输入框
- 实现功能条
- 实现参数快捷项弹层

输出物：

- 对话工作台前端页面
- 基础会话与消息结构

### 第五步：实现任务编排与模型适配主链路

目标：

- 打通前端请求到模型调用的主流程
- 接入意图识别逻辑
- 接入同步与异步任务机制
- 接入文件保存与记录写入

输出物：

- 任务管理模块
- 模型适配层
- 基础任务查询接口

### 第六步：优先打通文生图能力

目标：

- 实现文生图完整闭环
- 支持模型、比例、风格、模板、参考图等参数
- 支持结果展示、下载、再次生成、继续追问

输出物：

- 文生图能力全链路
- 对话流结果展示组件

### 第七步：扩展其余四类能力

目标：

- 接入图像识别
- 接入文生语音
- 接入文生 PPT
- 接入文生视频

输出物：

- 五类能力全部可用
- 对应结果展示和下载能力全部可用

### 第八步：完成调用记录与统计中心

目标：

- 实现记录列表
- 实现筛选查询
- 实现详情抽屉
- 实现结果下载
- 实现单条导出和列表导出
- 实现统计卡片和趋势分析

输出物：

- 调用记录中心
- 统计分析页面

### 第九步：补齐异常与体验优化

目标：

- 处理超时、失败、重试、兜底提示
- 优化上下文继承和参数沿用
- 优化异步任务轮询体验
- 优化 PostgreSQL 查询性能和索引策略

输出物：

- 稳定性增强版本
- 数据库优化版本

### 第十步：联调、验收、上线

目标：

- 与模型底座联调
- 完成验收测试
- 发布上线

输出物：

- 验收版本
- 上线版本

---

## 13. 最终结论

综合当前需求、项目定位以及数据库选型要求，推荐本项目采用：

**独立多模态应用系统 + React 前端 + Spring Boot 模块化单体后端 + PostgreSQL + Redis + RabbitMQ + MinIO + 异步任务编排 + 模型适配层 + 文件与记录统一治理**

该方案具有以下特点：

- 一期能快速落地
- 能满足当前五类能力接入与验收要求
- 能较好支撑动态参数、任务状态、消息记录、结果摘要等复杂数据场景
- 能充分利用 PostgreSQL 在 `jsonb`、复杂查询和统计分析方面的优势
- 能为未来能力扩展和架构演进保留空间

因此，这是一份在当前阶段最平衡、最稳妥、最具工程可实施性的推荐开发方案。
