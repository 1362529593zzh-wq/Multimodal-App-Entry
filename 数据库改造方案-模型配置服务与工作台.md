# 数据库改造方案：模型配置服务与工作台接入国内开源模型

## 1. 文档目的

本文专门说明数据库层需要如何改造，以支撑以下目标：

1. 在模型配置服务中导入中国国内开源大模型配置。
2. 让模型配置服务成为工作台的真实模型来源。
3. 保持前端 UI 排版继续按既有改造计划执行，不因为数据库设计不合理而返工。

本文只讨论数据库，不展开前端页面排版实现。

---

## 2. 当前数据库现状

当前项目数据库迁移文件位于：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V1__init_schema.sql`
- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V2__core_tables.sql`
- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V3__extra_tables.sql`
- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V4__indexes.sql`
- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V5__seed_data.sql`

当前已经有 3 张与本次目标最相关的表：

### 2.1 模型服务表

- 表名：`app.mm_model_service`
- 建表位置：`E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V2__core_tables.sql`

当前主要字段：

- `service_code`
- `service_name`
- `model_code`
- `model_name`
- `model_type`
- `function_code`
- `endpoint`
- `auth_type`
- `timeout_ms`
- `enabled`
- `publish_status`
- `is_default`
- `allow_front_select`
- `supported_options`
- `remark`

### 2.2 功能与模型绑定表

- 表名：`app.mm_function_model_binding`
- 建表位置：`E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V3__extra_tables.sql`

当前主要字段：

- `function_code`
- `service_code`
- `is_default`
- `sort_order`
- `enabled`
- `remark`

### 2.3 参数模板表

- 表名：`app.mm_param_template`
- 建表位置：`E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V3__extra_tables.sql`

当前主要字段：

- `template_code`
- `template_name`
- `function_code`
- `service_code`
- `template_type`
- `template_payload`
- `preset_params`
- `description`
- `sort_order`
- `enabled`
- `is_default`

---

## 3. 当前数据库设计的主要不足

### 3.1 `mm_model_service` 还不是“模型注册中心”

当前它更像一个基础模型配置表，还不具备以下能力：

- 没有厂商维度，无法做左侧厂商筛选。
- 没有 provider 协议类型，无法区分不同国内模型服务的调用方式。
- 没有密钥引用或脱敏字段，不利于安全接入。
- 没有自定义 header / payload 模板，无法适配非统一协议。

### 3.2 `mm_function_model_binding` 结构够用，但定位还不够明确

当前这张表已经存在，但后续应明确作为：

- 工作台某能力可用模型列表的主数据来源
- 工作台某能力默认模型的主数据来源

### 3.3 `mm_param_template` 可继续复用，但数据约定要更清晰

这张表已经很适合继续使用，不建议推翻。
但后续要更明确区分：

- `composer_schema`
- `prompt_template`

---

## 4. 改造原则

建议遵循以下原则：

1. 尽量保留现有三张核心表，不做大推翻。
2. 优先扩展 `mm_model_service`，不要一开始拆太多新表。
3. 让 `mm_function_model_binding` 成为运行时模型选择主表。
4. 第一阶段先把链路跑通，第二阶段再考虑更复杂的厂商字典和密钥分表。

---

## 5. 推荐新增的迁移文件

建议新增两个 migration：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V6__model_service_provider_upgrade.sql`
- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V7__model_service_provider_indexes.sql`

职责建议：

### 5.1 `V6__model_service_provider_upgrade.sql`

负责：

- 升级 `app.mm_model_service` 结构
- 回填旧数据默认值
- 可选创建密钥表

### 5.2 `V7__model_service_provider_indexes.sql`

负责：

- 给新增字段补索引
- 补唯一默认绑定约束索引

---

## 6. `mm_model_service` 应如何改造

### 6.1 建议新增字段

建议新增以下字段：

- `vendor_code varchar(64) not null default 'default_vendor'`
- `vendor_name varchar(128) not null default '默认厂商'`
- `vendor_type varchar(32) not null default 'official'`
- `provider_type varchar(64) not null default 'openai_compatible'`
- `secret_ref varchar(128)`
- `api_key_masked varchar(128)`
- `secret_key_masked varchar(128)`
- `request_method varchar(16) not null default 'POST'`
- `header_template jsonb not null default '{}'::jsonb`
- `payload_template jsonb not null default '{}'::jsonb`
- `extra_config jsonb not null default '{}'::jsonb`
- `sort_order integer not null default 0`

### 6.2 每个字段的用途

#### 厂商相关

- `vendor_code`
  - 给模型配置服务页左侧筛选使用
  - 建议使用稳定编码，如 `qwen`、`zhipu`、`deepseek`

- `vendor_name`
  - 给页面展示使用
  - 如 `通义千问`、`智谱`、`DeepSeek`

- `vendor_type`
  - 标识 `official` 或 `custom`
  - 用于页面中“已有厂商 / 自定义接入”的交互

#### 协议与调用相关

- `provider_type`
  - 决定工作台后端调用走哪个适配器
  - 建议第一阶段支持：
    - `openai_compatible`
    - `ollama`
    - `custom_http`

- `request_method`
  - 支持不同调用方式
  - 建议允许：
    - `GET`
    - `POST`
    - `PUT`
    - `PATCH`

- `header_template`
  - 自定义请求头模板
  - 用于特殊鉴权场景

- `payload_template`
  - 自定义请求体模板
  - 用于非统一协议适配

- `extra_config`
  - 存协议扩展参数
  - 如：
    - `responseMode`
    - `supportsReferenceImage`
    - `supportsStream`
    - `apiPath`

#### 密钥相关

- `secret_ref`
  - 推荐存密钥引用标识
  - 实际密钥可走环境变量或外部密钥中心

- `api_key_masked`
  - 返回给前端的脱敏 API Key 展示字段

- `secret_key_masked`
  - 返回给前端的脱敏 Secret Key 展示字段

#### 排序相关

- `sort_order`
  - 控制列表排序
  - 页面展示更稳定

### 6.3 推荐 SQL

建议写入 `V6__model_service_provider_upgrade.sql`：

```sql
ALTER TABLE app.mm_model_service
ADD COLUMN IF NOT EXISTS vendor_code varchar(64) NOT NULL DEFAULT 'default_vendor',
ADD COLUMN IF NOT EXISTS vendor_name varchar(128) NOT NULL DEFAULT '默认厂商',
ADD COLUMN IF NOT EXISTS vendor_type varchar(32) NOT NULL DEFAULT 'official',
ADD COLUMN IF NOT EXISTS provider_type varchar(64) NOT NULL DEFAULT 'openai_compatible',
ADD COLUMN IF NOT EXISTS secret_ref varchar(128),
ADD COLUMN IF NOT EXISTS api_key_masked varchar(128),
ADD COLUMN IF NOT EXISTS secret_key_masked varchar(128),
ADD COLUMN IF NOT EXISTS request_method varchar(16) NOT NULL DEFAULT 'POST',
ADD COLUMN IF NOT EXISTS header_template jsonb NOT NULL DEFAULT '{}'::jsonb,
ADD COLUMN IF NOT EXISTS payload_template jsonb NOT NULL DEFAULT '{}'::jsonb,
ADD COLUMN IF NOT EXISTS extra_config jsonb NOT NULL DEFAULT '{}'::jsonb,
ADD COLUMN IF NOT EXISTS sort_order integer NOT NULL DEFAULT 0;
```

---

## 7. 老数据如何回填

当前已有默认 seed 数据来自：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V5__seed_data.sql`

新增字段后，建议对旧数据做一次回填。

### 7.1 推荐回填方式

```sql
UPDATE app.mm_model_service
SET vendor_code = CASE function_code
    WHEN 'image_generation' THEN 'default_image_vendor'
    WHEN 'image_recognition' THEN 'default_vision_vendor'
    WHEN 'text_to_speech' THEN 'default_audio_vendor'
    WHEN 'text_to_ppt' THEN 'default_ppt_vendor'
    WHEN 'text_to_video' THEN 'default_video_vendor'
    ELSE 'default_vendor'
END
WHERE vendor_code = 'default_vendor';

UPDATE app.mm_model_service
SET vendor_name = CASE function_code
    WHEN 'image_generation' THEN '默认图像厂商'
    WHEN 'image_recognition' THEN '默认视觉厂商'
    WHEN 'text_to_speech' THEN '默认语音厂商'
    WHEN 'text_to_ppt' THEN '默认PPT厂商'
    WHEN 'text_to_video' THEN '默认视频厂商'
    ELSE '默认厂商'
END
WHERE vendor_name = '默认厂商';
```

### 7.2 为什么要回填

不回填会带来两个问题：

1. 模型配置服务左侧厂商筛选全是同一个占位值，前端看起来不可用。
2. 后续工作台如果按厂商、provider 分组展示，会出现历史脏数据。

---

## 8. `mm_model_service` 建议补哪些约束

如果你希望数据库层更稳，建议加 check 约束。

### 8.1 推荐约束

```sql
ALTER TABLE app.mm_model_service
ADD CONSTRAINT ck_mm_model_service_vendor_type
CHECK (vendor_type IN ('official', 'custom'));

ALTER TABLE app.mm_model_service
ADD CONSTRAINT ck_mm_model_service_provider_type
CHECK (provider_type IN ('openai_compatible', 'ollama', 'custom_http'));

ALTER TABLE app.mm_model_service
ADD CONSTRAINT ck_mm_model_service_request_method
CHECK (request_method IN ('GET', 'POST', 'PUT', 'PATCH'));

ALTER TABLE app.mm_model_service
ADD CONSTRAINT ck_mm_model_service_auth_type
CHECK (auth_type IS NULL OR auth_type IN ('none', 'bearer', 'api_key', 'basic'));
```

### 8.2 使用建议

- 如果你当前环境还在频繁试错，第一阶段可以先不加这些约束。
- 如果你准备让多人维护配置，建议尽早加，能减少脏数据。

---

## 9. 是否需要新增密钥表

### 9.1 推荐方案

如果你希望密钥管理更规范，建议新增：

- `app.mm_model_service_secret`

### 9.2 建议表结构

```sql
CREATE TABLE IF NOT EXISTS app.mm_model_service_secret (
    id bigserial PRIMARY KEY,
    service_code varchar(64) NOT NULL,
    secret_type varchar(32) NOT NULL,
    secret_ciphertext text NOT NULL,
    created_at timestamptz NOT NULL DEFAULT now(),
    updated_at timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT uk_mm_model_service_secret UNIQUE (service_code, secret_type)
);
```

### 9.3 触发器

```sql
DROP TRIGGER IF EXISTS trg_mm_model_service_secret_updated_at ON app.mm_model_service_secret;
CREATE TRIGGER trg_mm_model_service_secret_updated_at
BEFORE UPDATE ON app.mm_model_service_secret
FOR EACH ROW
EXECUTE FUNCTION app.set_updated_at();
```

### 9.4 第一阶段是否必须建

我的建议：

- 如果你现在目标是尽快跑通链路：可以先不建
- 如果你已经确定后续长期维护：建议建

第一阶段不建时的替代方案：

- `mm_model_service` 存 `secret_ref`
- 真实密钥走环境变量或外部配置中心

---

## 10. `mm_function_model_binding` 需要如何使用

### 10.1 表结构暂时够用

这张表目前不一定要新增字段。

现有字段已经支持：

- 一个能力绑定多个模型服务
- 指定默认模型
- 指定排序
- 控制启停

### 10.2 这张表的运行时规则要明确

后续工作台选模型建议严格按下面规则：

1. 某能力下有哪些可用模型，优先查 `mm_function_model_binding`
2. 默认模型优先取：
   - `is_default = true`
   - 且 `enabled = true`
3. 候选模型列表按 `sort_order` 排序

### 10.3 推荐补一个唯一默认索引

为了避免一个能力配置出多个默认模型，建议加部分唯一索引：

```sql
CREATE UNIQUE INDEX IF NOT EXISTS uk_mm_function_model_binding_default_per_function
ON app.mm_function_model_binding (function_code)
WHERE is_default = true AND enabled = true;
```

这条索引非常有价值，因为它能直接在数据库层阻止错误配置。

---

## 11. `mm_param_template` 应如何继续使用

### 11.1 结构上可以保留不动

这张表目前结构已经够支撑你的目标，不建议推翻。

### 11.2 数据约定要统一

后续建议明确两类模板：

- `template_type = 'composer_schema'`
- `template_type = 'prompt_template'`

### 11.3 用途定义

- `composer_schema`
  - 给工作台底部参数面板用
  - 决定字段、选项、占位提示、是否支持上传

- `prompt_template`
  - 给预设提示词或预设参数用

### 11.4 是否要更新旧数据

如果当前代码还依赖 `default`，则暂时不要全量改值。

建议流程：

1. 先改后端代码兼容新类型
2. 再执行旧数据修正

如需修正，可用：

```sql
UPDATE app.mm_param_template
SET template_type = 'composer_schema'
WHERE template_type = 'default';
```

---

## 12. 索引需要如何补

现有索引文件是：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V4__indexes.sql`

建议新增索引写到：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V7__model_service_provider_indexes.sql`

### 12.1 推荐索引 SQL

```sql
CREATE INDEX IF NOT EXISTS idx_mm_model_service_vendor_enabled
ON app.mm_model_service (vendor_code, enabled, sort_order);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_provider_enabled
ON app.mm_model_service (provider_type, enabled, sort_order);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_model_type_enabled
ON app.mm_model_service (model_type, enabled, sort_order);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_vendor_name
ON app.mm_model_service (vendor_name);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_extra_config_gin
ON app.mm_model_service USING gin (extra_config);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_header_template_gin
ON app.mm_model_service USING gin (header_template);

CREATE INDEX IF NOT EXISTS idx_mm_model_service_payload_template_gin
ON app.mm_model_service USING gin (payload_template);

CREATE UNIQUE INDEX IF NOT EXISTS uk_mm_function_model_binding_default_per_function
ON app.mm_function_model_binding (function_code)
WHERE is_default = true AND enabled = true;
```

### 12.2 为什么需要这些索引

- `vendor_code`：支撑模型配置服务页左侧厂商筛选。
- `provider_type`：支撑后端按协议类型管理模型。
- `model_type`：支撑不同能力视图过滤。
- `jsonb gin`：提升 `extra_config / header_template / payload_template` 的检索效率。

---

## 13. 种子数据建议如何处理

当前种子数据在：

- `E:\shupai\Multimodal App Entry\backend\src\main\resources\db\migration\V5__seed_data.sql`

### 13.1 不建议直接改 V5

因为 V5 已经是历史迁移，不建议回头改老 migration。

### 13.2 推荐做法

新增后续导入脚本或走后端导入接口，将国内模型写入数据库。

如果后续你要切换到真实国内模型服务，建议把旧 mock 服务置为非启用：

```sql
UPDATE app.mm_model_service
SET enabled = false,
    publish_status = 'OFFLINE'
WHERE service_code IN (
    'img-gen-default',
    'img-rec-default',
    'tts-default',
    'ppt-default',
    'video-default'
);
```

### 13.3 导入国内模型时应写入哪些表

一次导入建议至少写三部分数据：

1. `mm_model_service`
   - 写模型服务本身

2. `mm_function_model_binding`
   - 写能力与模型的关系

3. `mm_param_template`
   - 写工作台参数 schema 或预设模板

---

## 14. 推荐实施顺序

### 阶段 1：结构升级

1. 新增 `V6__model_service_provider_upgrade.sql`
2. 扩展 `mm_model_service`
3. 回填旧数据默认值
4. 可选新增密钥表

### 阶段 2：索引与约束

1. 新增 `V7__model_service_provider_indexes.sql`
2. 增加查询索引
3. 增加默认绑定唯一索引
4. 视情况增加 check 约束

### 阶段 3：数据导入

1. 将国内开源模型配置导入 `mm_model_service`
2. 将能力绑定写入 `mm_function_model_binding`
3. 将工作台 schema 写入 `mm_param_template`

### 阶段 4：工作台切换

1. 后端运行时优先读取 `mm_function_model_binding`
2. 工作台按绑定和模板读取模型候选与 schema

---

## 15. 最终建议结论

本次数据库改造的核心不是新建很多表，而是：

1. 把 `app.mm_model_service` 升级成真正的模型注册中心。
2. 把 `app.mm_function_model_binding` 明确为工作台选模型的主关系表。
3. 把 `app.mm_param_template` 明确为工作台 schema 和模板来源。

对你当前项目，最稳妥的数据库方案是：

- 保留现有三张核心表
- 新增 `V6` 做字段升级
- 新增 `V7` 做索引与约束
- 可选新增密钥表

这样做的结果是：

- 模型配置服务可以导入国内开源模型
- 工作台可以基于能力绑定真实选择模型
- 后续前端 UI 继续按你既定排版实现，不需要返工数据库

---

## 16. 下一步建议

如果你要我继续往下做，最合适的下一步是直接产出：

1. `V6__model_service_provider_upgrade.sql` 完整文件
2. `V7__model_service_provider_indexes.sql` 完整文件

这样你就可以直接进入开发和落库阶段。
