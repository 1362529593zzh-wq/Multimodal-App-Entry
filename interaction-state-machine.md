# 多模态应用交互状态机

## 目标

为了避免产品、前端、后端对同一页面行为理解不一致，需求中不仅要定义页面布局，还要明确不同状态下的交互规则。本文将这部分整理为一套轻量的交互状态机，用于指导设计和实现。

当前工作台结构已经明确：

- 左侧导航
- 中间对话流
- 底部输入区 + 功能条 + 参数下拉

目前仍需补齐的是：在不同场景下，页面到底如何变化，尤其是能力切换、任务执行、上下文继承这几类行为。

---

## 核心原则

不要把整个页面设计成一个巨大的状态机，而应拆成三层：

1. 编辑器状态：底部输入区当前处于什么编辑模式
2. 任务状态：一次提交后的执行生命周期
3. 继承状态：当前请求是否继承上一轮的能力、参数或结果资产

这样定义更容易让前端和后端对齐实现逻辑。

---

## 全局设计原则

### 1. 提交即冻结

用户点击发送后，当前选择的能力、模型、参数、引用素材应被固化为一份任务快照。

- 后续 UI 上的任何修改都不能影响这次已提交任务
- 提交后切换能力，只影响下一次请求

### 2. 手动选择优先级最高

如果用户明确手动选择了某个能力，例如图像生成或视频生成：

- 系统不应静默覆盖
- 可以给出提示，但不应自动切换能力

### 3. 自动识别只是兜底

如果用户没有选择能力，直接输入请求：

- 系统可以在本次提交时推断能力类型
- 该推断结果只作用于当前这次请求
- 不应永久锁定编辑器到该能力上

### 4. 参数按能力分别保存

每种能力都维护自己独立的一套草稿参数。

例如：

- 图像生成保留自己的风格、比例、参考图、模型等草稿
- 视频生成保留自己的时长、运动强度、画幅、模型等草稿

当用户切换能力时：

- 之前能力的草稿不丢失
- 新切换到的能力恢复自己上一次的草稿
- 不兼容参数不应强行跨能力带过去

### 5. 续接是新任务，不是修改旧任务

如果用户说：

> 把这张图做成视频

它在产品语义上是会话续接，但在执行语义上必须是一个新任务。

系统应创建一个新的任务，并关联上一轮结果，而不是修改上一轮任务本身。

---

## 1. 编辑器状态

编辑器状态只影响下一次提交。

### 1.1 默认态

状态：`default`

定义：

- 未选择任何能力
- 只有输入框可用
- 参数区收起

行为：

- 用户可以自由输入
- 系统在用户发送前，不预设能力类型

### 1.2 功能选中态

状态：`capability_selected`

定义：

- 用户手动选择了某个能力

行为：

- 功能条对应按钮高亮
- 展开对应参数面板
- 后续输入默认按照该能力理解，直到用户主动切换

### 1.3 功能自动识别态

状态：`capability_auto_resolved`

定义：

- 用户没有手动选能力
- 系统在提交时根据提示词推断出能力类型

行为：

- 本次请求按推断出的能力执行
- 提交后，编辑器可以进入该能力的“软激活”状态
- UI 应显示弱提示，例如“已识别为图像生成”

规则：

- 自动识别的优先级低于手动选择
- 用户任意一次显式切换都应立即覆盖自动状态

### 1.4 上下文续接态

状态：`follow_up_selected`

定义：

- 当前输入绑定了某个上一轮任务或结果资产

行为：

- UI 明确展示当前引用的是哪个来源
- 用户可以一键移除该引用
- 下一次提交会基于该上下文创建新任务

### 编辑器状态总结

- 编辑器状态只影响下一次提交
- 编辑器状态绝不能回写已提交任务

---

## 2. 任务状态

每一次提交都应拥有独立的任务生命周期。

### 任务状态定义

- `draft`：草稿态，尚未提交
- `queued`：已提交，排队中
- `running`：执行中
- `streaming`：流式返回中，可能包含文本、进度或中间结果
- `succeeded`：执行成功
- `failed`：执行失败
- `cancelled`：已取消

### UI 表现方式

不要使用整页阻塞式 loading。

建议在对话流中插入一张任务卡片，并让它随着任务状态变化而演进：

- 排队中：显示占位卡片
- 执行中：显示状态文案或阶段提示
- 流式中：持续更新文案、缩略图或阶段信息
- 完成后：替换为最终结果卡片
- 失败后：显示错误卡片和重试操作

### 最低可见阶段

即使后端没有提供精确进度，前端至少也应展示以下阶段：

- 排队中
- 准备中
- 生成中
- 完成或失败

如果没有真实进度：

- 优先展示阶段状态
- 不建议用虚假的百分比进度条

---

## 3. 继承状态

继承关系应被显式建模，而不是完全依赖隐式推断。

### 继承模式

- `none`：不继承
- `same_capability`：同能力续接
- `cross_capability`：跨能力续接
- `from_asset`：基于某个生成结果资产续接

这一层决定当前请求允许复用上一轮哪些信息。

---

## 能力继承规则

真正需要明确的，不是“是否继承”，而是“继承到什么程度”。

### 推荐继承矩阵

| 字段 | 同能力续接 | 跨能力续接 |
| --- | --- | --- |
| 功能类型 | 继承 | 不继承，应切换为新能力 |
| 模型 | 默认继承 | 默认不继承 |
| 风格 | 默认继承 | 可软继承，视场景决定 |
| 比例 / 画幅 | 默认继承 | 如兼容则映射继承 |
| 参考图 / 结果资产 | 若被引用则继承 | 若被引用则必须继承 |
| 高级参数，如 seed、steps、fps、时长 | 默认不继承，除非用户明确要求 | 不继承 |

### 强规则

只自动继承用户容易理解的参数。

不要自动继承底层技术参数，除非用户明确表达：

- “沿用刚才的设置”
- “保持完全一样的参数”

这样可以避免系统行为给用户造成“为什么又偷偷沿用了上次参数”的困惑。

---

## 典型场景规则

### 场景 1：用户选了“图像生成”，又切到“视频生成”

问题：

- 之前参数是否保留？

结论：

- 保留，但按能力分别保存

规则：

- 图像生成参数保存到图像草稿箱
- 视频生成参数保存到视频草稿箱
- 切换到某个能力时，恢复该能力自己的最近一次草稿

结果：

- 切走不丢
- 切回来恢复
- 不兼容参数不跨能力强带

### 场景 2：用户未选功能，直接输入“生成一张海报”

问题：

- 系统识别成图像生成后，要不要自动激活图像功能？

结论：

- 要，但只对本次提交生效；提交后编辑器进入软激活状态

规则：

- 本次请求在提交时被解析为图像生成
- 提交后，底部功能条进入“图像生成”的软激活状态
- UI 应给出弱提示，说明这是自动识别结果

关键区别：

- 手动激活是强状态
- 自动激活是弱状态

### 场景 3：用户已经生成了图片，又说“帮我做成视频”

问题：

- 这是上下文续接，还是一个新任务？

结论：

- 在会话语义上属于上下文续接
- 在执行语义上必须是新任务

建议字段：

- `parent_task_id`
- `source_asset_id`
- `resolved_capability = video_generation`

UI 应明确提示：

- 当前正在基于上一张图片生成视频
- 用户可以一键断开继承关系

### 场景 4：某个能力正在执行中

问题：

- 底部功能条还能不能切换？

结论：

- 可以切，但只影响下一次请求

规则：

- 当前执行中的任务必须锁定
- 编辑器本身仍可继续编辑
- 功能切换只作用于下一次提交

建议明确写入产品规则：

- 编辑器可变
- 任务快照不可变

如果后端支持并发：

- 用户可以边等边准备下一条，甚至继续提交

如果后端不支持并发：

- 用户仍可编辑输入区
- 发送按钮可禁用，或改为“加入队列”

不要因为一个任务在执行，就把整个底部编辑器锁死。

### 场景 5：长任务处理中

问题：

- 页面应该表现为 loading、排队、占位卡片，还是流式状态？

结论：

- 不使用整页 loading
- 统一使用对话流中的任务卡片表达

推荐流转：

- `queued -> running -> streaming/progress -> succeeded/failed`

这样用户始终能看到对话上下文，也更容易理解系统当前处于哪个阶段。

---

## 前后端必须对齐的数据对象

为了避免实现歧义，建议明确区分两个对象。

### A. ComposerDraft

归属：前端编辑器

建议字段：

- `selected_capability`：当前选择的能力，可为空
- `selection_mode`：`manual | auto`
- `draft_text`
- `drafts_by_capability`
- `inherited_from_task_id`
- `inherited_asset_ids`
- `inheritance_enabled`

作用：

- 只描述当前可编辑的草稿状态
- 在提交前允许自由变化

### B. TaskSnapshot

归属：前后端共享任务事实

建议字段：

- `task_id`
- `parent_task_id`
- `resolved_capability`
- `resolved_model`
- `resolved_params`
- `source_asset_ids`
- `prompt`
- `inheritance_mode`
- `status`

作用：

- 表示一次真正提交出去的任务
- 提交后必须保持稳定，不可被编辑器后续变化影响

### 合同规则

前端上的所有切换和编辑，操作的是 `ComposerDraft`。

用户点击发送后，系统生成一份 `TaskSnapshot`。

这条区分非常关键，它可以避免以下歧义：

- 用户发送后又切换了能力
- 前端和后端对最终实际执行的参数理解不一致

---

## 建议写进 PRD 的硬规则

以下规则应在需求文档中明确写出：

1. 功能切换采用“按能力保存草稿”，不跨能力强继承不兼容参数
2. 自动识别只解析当前提交，并在提交后软激活对应能力
3. 基于上一轮结果继续生成，属于同会话中的新任务
4. 执行中允许继续编辑下一条，但不能修改已提交任务快照
5. 长任务统一以对话流任务卡片表达，不使用整页阻塞式 loading

---

## 建议提供的用户控制项

为了让“继承”行为对用户更可理解，建议在编辑器中至少提供以下操作：

### 清空输入

- 只清除文本内容

### 清空参数

- 保留当前能力
- 重置该能力下的参数

### 新任务

- 断开上下文继承
- 移除已引用的结果资产
- 停止继承上一轮能力参数

这些控制项能显著降低歧义，也能增强用户对系统行为的可控感。

---

## 最终建议

这部分内容应作为一个独立章节写入产品需求文档，而不是分散在页面说明里。

建议 PRD 结构如下：

1. 状态定义
2. 状态流转
3. 继承规则
4. 前后端字段约定
5. 边界场景与异常处理

如果这套规则在实现前就对齐，前端和后端会拥有一致的心智模型，后续联调成本也会明显更低。
